# 安全报告

## 范围

当前维护分支为 `main`，当前发行系列为 `1.0.x`。安全问题主要涉及随附第三方库、本地 HTTP 服务、资源加载和发行附件完整性。项目不提供账号、付费、票务或公网上传服务。

## 报告渠道

仓库当前为私有。有访问权限的成员可以在私有仓库创建 Issue，标题标注“安全问题”，说明受影响版本、组件、触发条件和影响范围，并提醒仓库所有者。该 Issue 对仓库授权成员可见，不是只对所有者可见的保密通道。

若内容包含仍可利用的攻击细节、个人数据或其他秘密，请先通过你与项目所有者已有的私密联系渠道协调，再提供材料。不要将密码、访问令牌、完整浏览器配置或真实用户数据贴入 Issue、截图或日志。没有仓库权限时，通过原先获得项目的联系人反馈；当前不提供公开安全报告邮箱。

## 处理与修订

维护者确认问题后，应记录受影响版本、修复范围与验证方式，并在修订发行中说明必要的升级措施。项目不承诺固定响应时限，也没有漏洞奖励计划。

发行附件可使用随附 `SHA256SUMS.txt` 核对完整性。本地服务用于本机预览；需要远程访问时应另行评估服务器与访问控制，不将本地预览工具直接当作受保护的线上服务。
