# 贡献指南

欢迎通过 Issue 讨论问题与改进，也欢迎提交 Pull Request。原创内容采用 MIT 许可；第三方库、素材和数据遵守各自条款，详见 [许可范围](./LICENSE.md)。

## 开始前

先阅读 [架构](./docs/ARCHITECTURE.md)、[已知限制](./docs/ROADMAP.md) 和现有 Issues。较大的功能、模型替换或依赖更新先描述目标、受影响模块和验证方法，避免同时改变许多无关内容。

本地需要 Node.js 22+。直接体验已提交的 `dist/` 可运行 `npm run dev`，无需安装 npm 依赖；网站源码、模型与第三方库都在项目中。

开发时先运行 `npm ci` 安装锁定的 esbuild 工具。修改 JavaScript 或 CSS 后运行 `npm run build`，它会更新主入口 bundle、提取的许可声明和两份入口 HTML 中的内联样式。提交源码时一并提交生成结果，不要直接编辑 `dist/app.bundle.js`。

## 提交修改

1. Fork [项目仓库](https://github.com/JohnnyGuo132/shanghai-disney-explorer)，克隆到本地。
2. 从 `main` 创建聚焦本次改动的分支，完成修改和验证。
3. 提交 Pull Request，说明解决的问题、具体变化、验证结果及相关 Issue。

提交贡献即表示你有权提供这些内容，并同意原创贡献按本项目的 MIT 许可分发。引入第三方内容时必须保留其许可与署名，并说明来源；不要将第三方内容重新声明为自己的 MIT 原创内容。

## 修改要求

- 一次 PR 聚焦一个可解释的变化，说明触发条件以及修改前后的行为。
- 保持资源相对路径和文件名大小写一致；不得依赖个人绝对路径或本地开发端口。
- 使用已有模块职责，不把所有功能继续堆入入口文件。
- 修改材质、灯光或模型时，同时检查白天/夜间、近景/鸟瞰和两档画质。
- 修改导航时，检查当前位置接入、暂停继续、到达状态与不可通行区域。
- 修改 UI 时，检查键盘焦点、窄屏、可读文字和按钮标签。
- 修改加载流程时，检查轻量／完整纹理、首次访问／缓存访问、加载失败重试，以及首帧显示后才撤下加载界面。
- 新素材必须记录作者、原始链接、许可、修改方式和随附声明；不要仅写“来自网络”。
- 第三方文件升级需要保留原有许可，并说明版本与兼容性影响。
- 截图必须来自当前代码实际运行，不能用概念图代替已经实现的功能。

## 提交前验证

```sh
npm ci
npm run build
npm run check
npm test
```

CI 运行 `npm run build -- --check` 核对已提交产物与源码一致，不会自动补交生成文件。直接使用现成 `dist/` 的用户不需要执行开发构建步骤。

文件检查不覆盖全部交互。PR 描述应补充实际浏览器、视口、画质和复现步骤；视觉改动附同角度前后截图，性能改动附可复测条件。无需为纯文字调整编写重复实现的测试。

手机加载优化应区分 HTML 首字节、页面首屏、场景首帧与稳定运行阶段，并注明网络、缓存、纹理与画质档位。桌面窄屏模拟或本机网络耗时不能代替 iOS / Android 真机基准，不用文件体积下降直接推算用户的等待时间。

不要提交密码、访问令牌、个人路径、缓存、临时调试输出、发行附件或完整历史备份。发行包由维护者在最终提交上生成，流程见 [发行说明](./docs/RELEASING.md)。

## 报告问题

使用 [问题模板](https://github.com/JohnnyGuo132/shanghai-disney-explorer/issues/new/choose) 描述版本、环境、步骤、期望结果与实际结果。安全相关问题按照 [SECURITY.md](./SECURITY.md) 处理。

讨论聚焦可复现事实和改进方案，尊重不同设备与使用经验。未经验证的画质或性能承诺不要写进 README 或发行说明。
